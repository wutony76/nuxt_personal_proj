# add-admin-chat-broadcast

後台總覽（`/admin`）新增「管理者發言」與「聊天室排程」：管理員可在後台直接以「管理者: {name}」身分發言到全站聊天室，也可以設定到點自動發送（例如每天早安訊息），到點由伺服器排程觸發、以「管理者: 排程」名義發送。本次是對既有 `add-socket-social` 聊天室系統的擴充，不新增獨立的聊天頻道。

**注意**：此文件是在功能已經實作完成後補寫的（實作與規劃並行進行），記錄的是實際落地的行為，補文件的目的是讓這個變更可被日後追溯、審查。
